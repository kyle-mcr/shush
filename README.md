# Shush

A tiny, single-purpose React app that plays a continuous soothing shush over soft white noise.

## Run locally

```bash
npm install
npm run dev
```

## Deploy

Push the project to a GitHub repository with a `main` branch. In the repository's **Settings → Pages**, select **GitHub Actions** as the source. Each push to `main` will build and publish the app.
